# Findings Register — Plug A Pro Platform Audit (2026-07-06)

All findings are code-verified with file:line evidence. Severity: Critical / High / Medium / Low. "Blocks" = blocks MVP scale (wider client acquisition) as-is.

**Priority key:** P0 must fix before wider client acquisition · P1 before onboarding many more providers · P2 pilot hardening · P3 later. See [BACKLOG_RECOMMENDATIONS.md](./BACKLOG_RECOMMENDATIONS.md) for the sequenced plan.

**Suggested owners:** `payments` (payment/PSP code), `messaging` (WhatsApp layer), `matching` (dispatch/leads), `platform` (infra/observability/architecture), `admin` (console), `security`.

---

## Customer Journey (CJ)

| ID | Sev | Type | Finding | Evidence | Recommended fix | Owner | Pri | Blocks |
|---|---|---|---|---|---|---|---|---|
| CJ-01 | Critical | Defect | Online payment links generated but never delivered: Peach `checkoutUrl` persisted then discarded (only caller is fire-and-forget); Pay@Go returns a ready `whatsappMessage` with pay link that nothing sends. Default `bypass` mode masks this — in checkout mode customers cannot pay. | `lib/matching/service.ts:3082`, `lib/payat-go/booking-payments.ts:268`, `lib/payments.ts:77-79` | Send checkoutUrl/Pay@Go link via WhatsApp on booking creation | payments | P0 | Yes |
| CJ-02 | Critical | Data | Ratings do nothing: `Provider.averageRating` read by matching/shortlists but never recomputed from Review rows; review data forked (matchId-only reviews invisible to jobId-based profile aggregation and follow-up dedup). Trust loop is dead. | `lib/matching/candidate-pool.ts:227`, `app/review/[token]/actions.ts:11`, `app/(provider)/provider/profile/page.tsx:121-129` | Recompute averageRating on review submit; unify reviews on jobId+matchId | matching | P0 | Yes |
| CJ-03 | Critical | Reliability | Post-match "provider accepted" notice silently no-ops when customer is outside the 24h window and template unapproved; only backstop is a flag-gated cron firing 6–72h later. Matches the matched-not-told client incident. | `lib/post-match-communications.ts:400-404`, `app/api/cron/customer-match-confirmation-nudge/route.ts:20,43-47` | Approve the template; un-gate + tighten the nudge cron; alert on `NO_ACTIVE_WHATSAPP_SERVICE_WINDOW` outcomes | messaging | P0 | Yes |
| CJ-04 | Critical | UX | WhatsApp customer support is a dead-end: "dispute/issue" keywords route to the provider-only flow; help-menu promises follow-up "within 2 hours" but nothing captures the reply, creates a Dispute/Case, or notifies ops. | `lib/whatsapp-bot.ts:2338-2342`, `lib/whatsapp-flows/help.ts:133-155` | Customer trigger for `openCase` + ops notification on `faq_problem_with_job` | messaging | P0 | Yes |
| CJ-05 | High | Defect | WhatsApp cancel only cancels JobRequests; a customer with a confirmed Booking is told "no active job requests to cancel". `cancelBookingLifecycle` (refund + both-party notify) unreachable from the customer WhatsApp path; provider never notified on the raw path. | `lib/whatsapp-bot.ts:3115-3144`, `lib/bookings.ts:69-251` | Route WhatsApp cancel to `cancelBookingLifecycle` when a booking exists | messaging | P0 | Yes |
| CJ-06 | High | Reliability | Reschedule is a logged promise: writes an audit row + pings `ADMIN_WHATSAPP_NUMBER` (vanishes if unset) while telling the customer "our team will confirm shortly"; provider never notified; no rescheduleCount limit enforced. | `lib/bookings.ts:302-322`, `lib/whatsapp-bot.ts:3090-3093` | Create an ops-queue item (durable) + notify provider; enforce reschedule limit | messaging | P0 | Yes |
| CJ-07 | High | Defect | Stub R0 quotes live: post-lock fulfilment mints PENDING quotes with amount=0, validUntil=null; expiry sweep skips them; `/quotes/[token]` renders Accept/Decline; accept fails with generic `MISSING_PREFERRED_DATE`. | `lib/post-lock-fulfilment.ts:69-99`, `lib/quotes.ts:46-55,177`, `app/quotes/[token]/page.tsx:36-50` | Guard the quote page for stub quotes ("awaiting provider's quote"); exclude R0 from decision path | matching | P0 | Yes |
| CJ-08 | High | UX | Requests strand silently: no nudge/expiry for PENDING_VALIDATION (customer never picked a matching mode), SHORTLIST_READY, or PROVIDER_CONFIRMATION_PENDING — `expireOpenJobRequest` only covers OPEN\|MATCHING. Mid-funnel 100%-drop pattern already observed in prod. | `lib/job-requests/expire-job-request.ts:40`, `app/api/cron/customer-abandoned-recovery/route.ts:30-46` | Extend expiry sweep + nudges to all three statuses | matching | P0 | Yes |
| CJ-09 | High | UX | No-supply silence: on EMPTY_POOL/STRUCTURAL NO_MATCH the customer gets nothing until expiresAt; with a prior dispatch decision even immediate-expiry is skipped. Hours of silence in out-of-supply areas. | `lib/job-requests/create-job-request.ts:661-669`, `lib/matching/orchestrator.ts:271-279` | Immediate honest "no providers available" message + waitlist offer | matching | P0 | Yes |
| CJ-10 | High | Reliability | Silent job transitions: PAUSED, FAILED, CANCELLED, CALLBACK_REQUIRED send no customer message (FAILED only opens an internal case). | `lib/jobs.ts:123-244` | Add customer notifications for all four transitions | messaging | P1 | Yes |
| CJ-11 | High | Reliability | Jobs hang in PENDING_COMPLETION_CONFIRMATION if the customer never signs off; completion-check cron only covers AUTO_ASSIGN and is flag-gated. | `lib/completion-check.ts:10-13` | Auto-complete after N days + nudges; cover all assignment modes | matching | P1 | Yes |
| CJ-12 | High | UX | Shipped stub: `(client)` PWA "New request" page tells authed customers to "use WhatsApp in the meantime" while the real web flow exists at `/book/[serviceId]`. | `components/client/new-request-wizard.tsx:12` | Redirect to `/book/[serviceId]` picker | platform | P1 | No |
| CJ-13 | High | Defect | Payment failure has no retry path ("please try again" with no link/mechanism); payment-success-after-cancel keeps money, books nothing, never messages the customer (ops queue only). | `lib/client-pwa-submission-notifications.ts:103-128`, `lib/payments.ts:483-536` | Include a fresh checkout link in failure message; customer comms on post-cancel success | payments | P1 | Yes |
| CJ-14 | Medium | Data | Customer ad attribution dead-ends: `ctwaClid` captured but never attached to customer CAPI events; marketing wa.me links strip UTM/click IDs. Paid customer acquisition unmeasurable. | `lib/whatsapp-referral.ts:68`, `marketing/lib/whatsapp.ts:13` | Customer audience in referral classification + CAPI Lead emit | platform | P2 | No |
| CJ-15 | Medium | Defect | Inconsistent phone matching: status/cancel/reschedule/relay use exact `findUnique({phone})` while creation uses `phoneLookupVariants`; quote phone binding also exact. Legacy-format rows break "my requests" and quote buttons. | `lib/whatsapp-flows/status.ts:163-165`, `lib/whatsapp-bot.ts:2999,3107`, `lib/quotes.ts:172-173` | Use `phoneLookupVariants` everywhere | messaging | P1 | No |
| CJ-16 | Medium | Defect | West Rand pilot gate enforced on web quote approval but not the WhatsApp `quote_accept_` path — channel-dependent policy. | `app/api/quotes/[token]/route.ts:149-173` vs `lib/whatsapp-bot.ts:3586-3649` | Apply the gate in `processQuoteDecision` (single choke point) | matching | P2 | No |
| CJ-17 | Medium | UX | Customer never chooses a booking time: date = provider's `quote.preferredDate`; WhatsApp slot-picker is a deleted stub; alt-slot flow footer overpromises ("books you in immediately" but only re-opens matching). | `lib/quotes.ts:228-233`, `lib/whatsapp-flows/booking.ts:1-6`, `alternative-slot.ts:181,266-286` | Fix copy now; slot confirmation step post-pilot | matching | P2 | No |
| CJ-18 | Medium | Reliability | Auto-created bookings send no confirmation at creation; confirmation only arrives via payment webhook which never fires in bypass mode. | `lib/matching/service.ts:3082`, `app/api/webhooks/payments/route.ts:99` | Send confirmation at booking creation in bypass mode | payments | P1 | No |
| CJ-19 | Medium | Reliability | Quotes silently lapse EXPIRED with no notice to customer or provider; only one rating nudge exists and its CTA points at the ticket URL, not the review page. | `lib/quotes.ts:46-55`, `app/api/cron/follow-up/route.ts:74,29-32` | Pre-expiry quote nudges both ways; fix review CTA | messaging | P2 | No |
| CJ-20 | Medium | Data | `ServiceAreaWaitlist` is write-only: "we'll WhatsApp you the moment we go live" promised, but nothing ever re-contacts waitlist rows when a region activates. | `lib/whatsapp-flows/job-request.ts:1691-1696` | Region-activation job that drains the waitlist | matching | P2 | No |
| CJ-21 | Medium | Reliability | Refund failures swallowed (console.error only); Pay@Go refunds always return success:false by design with no ops alert. | `lib/bookings.ts:198-200`, `lib/payments.ts:272-279` | Ops-queue item on refund failure | payments | P2 | No |
| CJ-22 | Medium | UX | Mediated relay swallows idle free text: any ≥2-char message from a customer with an active booking forwards to the provider (support questions, "thanks"); target = most recent active across all bookings. | `lib/whatsapp-bot.ts:2456-2463,2614-2749` | Menu-first interception for common intents; explicit relay opt-in | messaging | P2 | No |
| CJ-23 | Medium | Security | Quote `approvalToken` has no TTL/revocation (soft validUntil only) — asymmetric with the hardened 72h revocable customerAccessToken. (= SEC-05) | `app/quotes/[token]/page.tsx:30-34` vs `lib/job-request-access.ts:12-86` | 404 the GET after decision + N days | security | P2 | No |
| CJ-24 | Medium | UX | Web dead-ends: non-pilot categories 404 at `/book/[serviceId]`; marketing onboarding form 500s to the user after the lead was already saved if the intake insert fails. | `app/(customer)/book/[serviceId]/page.tsx:37-52`, `marketing/app/api/leads/route.ts:176-197` | Friendly out-of-pilot page; decouple intake insert from response | platform | P2 | No |
| CJ-25 | Medium | Observability | Zero smoke coverage of the token surfaces customers actually use: `/requests/access/[token]`, `/quotes/[token]`, `/review/[token]`, `/approve/[token]`, `/confirm-completion/[token]`. | `lib/admin-nav-routes.ts:84-93` | Add invalid-token smoke checks for all five | platform | P1 | No |
| CJ-26 | Low | UX | Premature "match found" copy at dispatch (before any provider accepts); hardcoded "approximately 20 minutes" ETA on EN_ROUTE. | `lib/matching/orchestrator.ts:450`, `lib/jobs.ts:167-175` | Copy fixes | messaging | P3 | No |
| CJ-27 | Low | Data | Invoices pull-only (never pushed to customer; gated to COMPLETED + OWNER member); `taxAmount` always null. Vouchers provider-wallet-only — customers cannot apply discounts. | `app/api/customer/bookings/[id]/invoice/route.ts:30,57`, `lib/invoice/generate.tsx:98` | Push invoice link on completion; tax handling before VAT registration | payments | P3 | No |
| CJ-28 | Low | Maintainability | Dead/duplicated plumbing: `lib/client-pwa-handoff.ts` (tests only), two `resolveClientPwaDestination` implementations, unused `lib/booking-cancel-reasons.ts`. | `lib/server/client.ts:365` vs `lib/client-pwa-destination.ts` | Delete dead code | platform | P3 | No |

## Admin / Operations (AD)

| ID | Sev | Type | Finding | Evidence | Recommended fix | Owner | Pri | Blocks |
|---|---|---|---|---|---|---|---|---|
| AD-01 | High | Defect | Message "Retry" and admin broadcast flip/create `MessageEvent.status='QUEUED'` but **nothing consumes QUEUED** — no worker, cron, or code path re-sends. Retry also clears failureReason, hiding the message from the FAILED list. Ops believe failed comms were retried; they weren't. | `app/(admin)/admin/messages/actions.ts:49-56,231-245`; exhaustive grep: zero QUEUED consumers | Re-send inline on retry; add a QUEUED-drain cron for broadcast | admin | P0 | Yes |
| AD-02 | — | Resolved | Both suspected provider-book bugs are FIXED (commit 2ab5de90): reject accepts PENDING\|MORE_INFO_REQUIRED, reason required (min 5) + stored + sent to provider. Memory/CLAUDE.md were stale. | `app/(admin)/admin/applications/page.tsx:634-723,1340-1347` | Update CLAUDE.md/memory | — | — | No |
| AD-03 | Medium | UX/Obs | "Customer says paid but no booking" not diagnosable from the payments page: Payment requires bookingId; no search by name/phone/PSP reference; reconcile only flips existing rows. | `app/(admin)/admin/payments/page.tsx:169-212`, `actions.ts:41-59` | Add payment/PSP-reference search + unmatched-payment intake | admin | P1 | No |
| AD-04 | Medium | Security | Role floors are linear (OPS<FINANCE<TRUST<ADMIN<OWNER): TRUST can issue refunds/write-offs; FINANCE/TRUST inherit every OPS capability. `roleExact` exists but unused on finance mutations. | `lib/crud-action.ts:33-49,57-65`, `app/(admin)/admin/payments/actions.ts:75,131` | `roleExact: true` on refund/write-off/reconcile | security | P2 | No |
| AD-05 | Low | Security | `seedDefaultVendorConfigs` is an exported `'use server'` action with no auth, no crudAction, no audit — compiles to an invokable endpoint (create-only defaults, bounded impact). | `app/(admin)/admin/verifications/vendors/actions.ts:91-104` | Gate with requireRole(['OWNER']) or move to scripts/ | security | P1 | No |
| AD-06 | Low | Obs | Dispatch `redispatch`/`escalateToSupply` bypass crudAction: requireAdmin only, no role floor/flag/audit; CaseEvent write is best-effort `.catch(()=>{})`. Redispatch re-triggers matching (leads, WhatsApp sends) with no audit row. | `app/(admin)/admin/dispatch/actions.ts:75-156` | Wrap both in crudAction like `overrideAssignmentAction` | admin | P2 | No |
| AD-07 | Low | Obs | Case-lifecycle mutations (resolve/reopen/claim/note) call `lib/cases` directly — no AuditLog; trail exists only as CaseEvents. Parallel `_actions/case/index.ts` **does** wrap crudAction — two conventions coexist. | `app/(admin)/admin/dispatch/page.tsx:248-335`, `validation/page.tsx:240-305` | Consolidate on the `_actions/case` path | admin | P2 | No |
| AD-08 | Medium | UX | Messages page: last 100 outbound events only, no pagination/search/status filter, no inbound view. A failed send older than 100 events is invisible; <1 day of traffic at scale. | `app/(admin)/admin/messages/page.tsx:45-52` | Phone/status filters + pagination + inbound tab | admin | P1 | Yes |
| AD-09 | Low | UX | `/admin/settings` and `/admin/services` are static stubs with hardcoded 8-category lists while `/admin/categories` manages the real model. **No feature-flag management UI anywhere** — every kill-switch needs DB/script access. | `app/(admin)/admin/settings/page.tsx:40-49`, `services/page.tsx:11-43` | OWNER-gated flag toggle UI; delete/redirect stubs | admin | P2 | No |
| AD-10 | Low | Docs | CLAUDE.md materially stale: 3 dead routes listed, ~14 new routes missing, OWNER guards + DB-backed proxy auth + Category model all now exist (doc says they don't). Stale docs mislead future work. | CLAUDE.md vs route tree | Refresh CLAUDE.md | platform | P1 | No |
| AD-13 | Low | UX | `AssignmentHold` has no admin surface (aggregate counters only): a stuck hold can't be inspected or cancelled directly. | `app/(admin)/admin/page.tsx:255-269` | Holds list + cancel action on dispatch page | admin | P3 | No |

## SRE / Reliability (SRE)

| ID | Sev | Type | Finding | Evidence | Recommended fix | Owner | Pri | Blocks |
|---|---|---|---|---|---|---|---|---|
| SRE-01 | High | Defect | Late/duplicate `payment.failed` clobbers a PAID payment: `handlePaymentFailed` updates status FAILED with no status guard (route idempotency covers success only); fires a bogus "payment failed" customer message + ops item. Unknown bookingId on failed path throws P2025 → 500 → PSP retry loop. | `lib/payments.ts:539-547`, `app/api/webhooks/payments/route.ts:44-78` | Guarded `updateMany({where:{status:{notIn:['PAID','REFUNDED']}}})` + existence check | payments | P0 | Yes |
| SRE-02 | High | Reliability | Booking confirmation permanently lost if the send fails post-PAID: send throws → 500 → PSP retry hits the PAID early-return → never re-attempted, no sentinel records the miss. | `app/api/webhooks/payments/route.ts:73-106` | Sentinel column (like `matchFoundWhatsappSentAt`) + cron re-drive | payments | P0 | Yes |
| SRE-03 | High | Reliability | Failed inbound WhatsApp messages dead-lettered with no re-drive: processing runs in `after()` non-transactionally; on error only failureReason recorded; no cron sweeps `processedAt:null` rows — the dedup row actively blocks Meta redelivery. Customer replies can silently vanish. | `app/api/webhooks/whatsapp/route.ts:131-140`; `lib/whatsapp-bot.ts` (zero $transaction) | Retry sweep over `processedAt IS NULL AND failureReason IS NOT NULL` | messaging | P0 | Yes |
| SRE-04 | High | Data | Paid-but-unrecorded checkout possible: PSP session created before the Payment row upsert; if upsert fails, customer can pay against a checkout with no Payment row; success webhook 400s "Not found" until PSP stops retrying. | `lib/payments.ts:298-325`, `webhooks/payments/route.ts:55-58` | Upsert PENDING Payment before creating the PSP session | payments | P0 | Yes |
| SRE-05 | Medium | Defect | Refund desync: PSP refund first, DB update second, no transaction/retry; inbound `payment.refunded` webhooks deliberately ignored — PSP-side refunds/chargebacks never reflect. | `lib/payments.ts:600-629`, `webhooks/payments/route.ts:111` | Two-phase refund record; handle refund webhooks | payments | P1 | No |
| SRE-06 | Medium | Reliability | No timeout/retry/circuit-breaker on Meta and Peach fetches (bare `fetch`, no AbortSignal). A hung endpoint pins function time; crons doing sequential sends can blow their budget mid-batch. Didit client does it right (AbortSignal.timeout + 1 retry). | `lib/whatsapp.ts:128,178`, `lib/payments.ts:119,196` vs `lib/identity-verification/vendors/didit/client.ts:59-82` | `AbortSignal.timeout` + single retry helper for all external fetches | platform | P0 | Yes |
| SRE-07 | Medium | Defect | Peach webhook HMAC with empty secret: constructor doesn't enforce `PEACH_WEBHOOK_SECRET`, defaults to `''` — signatures forgeable if env var unset. (= SEC-02) | `lib/payments.ts:111,156-173` | Fail closed on empty webhookSecret (mirror `lib/webhook-auth.ts:13-17`) | security | P0 | No* |
| SRE-08 | Medium | Reliability | Provider double-booking: no overlap check inside the quote-approval transaction, no DB exclusion constraint on provider time windows. Two concurrent quote approvals can double-book a provider. | `lib/quotes.ts:86-135`, `lib/slotting.ts:47-60` | Overlap re-check on scheduleItem inside the tx, or Postgres exclusion constraint | matching | P1 | At scale |
| SRE-09 | Low | Reliability | API input validation mostly hand-rolled: ~8 of 110 route files use schema parse; zod in just 2 API routes. Webhooks signature-gated first (mitigates). | e.g. `app/api/provider/assignment-offers/[id]/accept/route.ts:26` | Standard `parseBody(schema)` helper | platform | P2 | No |
| SRE-10 | Low | Reliability | Rate-limiter memory fallback is per-instance (toothless on serverless); OTP fails CLOSED in prod when Redis is down (availability risk); `provider/registration/submit` + payment webhooks have no rate limit (signature-gated, acceptable). | `lib/rate-limit.ts:154-282` | Confirm Upstash env in prod; alert on `rate_limit.degraded` | security | P1 | If env missing |

*SRE-07/SEC-02: blocks scale only if `PEACH_WEBHOOK_SECRET` is unset in prod — verify today.

Positive verifications: WhatsApp webhook signature (HMAC + timingSafeEqual, fail-closed) + always-200 fast-ack (no Meta retry storm); Didit webhook 3-tier HMAC + idempotency + replay-safe; all 17+ cron routes check CRON_SECRET; expiry sweeps exist for leads/offers/quotes/holds/payment-intents; credit deduction/lead-lock transaction design is exemplary; 24h-window handling (PR #157) verified real.

## Observability / Traceability (OBS)

| ID | Sev | Type | Finding | Evidence | Recommended fix | Owner | Pri | Blocks |
|---|---|---|---|---|---|---|---|---|
| OBS-01 | High | Obs | Client-side Sentry almost certainly dead in prod: Turbopack doesn't load `sentry.client.config.ts` (needs `instrumentation-client.ts`, absent); CSP connect-src omits `*.sentry.io`. Browser errors in customer/provider flows invisible. | `next.config.ts:15,48` | Add `instrumentation-client.ts`; add Sentry ingest host to CSP | platform | P0 | Yes |
| OBS-02 | Medium | Obs | No `onRequestError` export in instrumentation.ts and no `app/global-error.tsx` — nested RSC/request errors and root-layout crashes bypass Sentry. | `instrumentation.ts` | `export const onRequestError = Sentry.captureRequestError`; add global-error.tsx | platform | P0 | No |
| OBS-03 | High | Obs | WorkflowEvent funnel ends at CLIENT_NOTIFIED: MATCH_CREATED, CUSTOMER_ACCEPTED_MATCH, JOB_SCHEDULED/STARTED/COMPLETED, PAYMENT_COMPLETED/FAILED, INVOICE_ISSUED, REVIEW_SUBMITTED defined but never emitted. Funnel can't measure acceptance→booking→completion→payment — the segment where prior client failures occurred. | `prisma/schema.prisma:3363-3391` vs 10 emit sites | Emit from quotes.ts booking tx, jobs.ts transitions, payments.ts handlers | platform | P1 | Yes |
| OBS-04 | Medium | Obs | No logger abstraction (~745 raw console.* sites); correlationId used in 3 routes, never persisted to any DB row. Cross-request tracing depends on Vercel log retention. | `lib/correlation.ts`; grep counts | Persist correlationId into event rows; one thin structured logger | platform | P2 | No |
| OBS-05 | Medium | Reliability | PSP payment webhook payloads not persisted (console only) — unlike KYC which stores redacted raw payloads. Payment disputes unreconstructable after log expiry. | `app/api/webhooks/payments/route.ts` vs `verification/[vendor]/route.ts:44` | PaymentWebhookEvent table mirroring ProviderVerificationWebhookEvent | payments | P1 | Once checkout on |
| OBS-06 | Medium | Obs | MessageEvent lacks jobRequestId/matchId FKs; customerId null when phone doesn't resolve. Pre-booking comms can't join to the request story. | `prisma/schema.prisma:1887-1912`, `lib/message-events.ts:18-21` | Additive nullable jobRequestId column | messaging | P2 | No |
| OBS-07 | Medium | Obs | crudAction doesn't populate AuditLog.ipAddress/userAgent though columns exist. | `lib/crud-action.ts:207-218` | Thread request headers into crudAction | admin | P2 | No |
| OBS-08 | Low | Obs | Booking creation writes no BookingStatusEvent/WorkflowEvent — timeline starts mid-story. | `lib/quotes.ts:86-107` | CREATED→SCHEDULED event in the same tx | matching | P2 | No |
| OBS-09 | Medium | Reliability | All alerting = best-effort WhatsApp to ADMIN_WHATSAPP_NUMBER (silently skipped if unset); 21 crons with no failure hook/heartbeat; no uptime monitor. **A dead match-leads cron silently stalls the whole marketplace.** | `lib/whatsapp.ts:1642-1696`, `vercel.json` | External uptime check on /api/health + cron heartbeats + stale-heartbeat alert | platform | P0 | Yes |
| OBS-10 | Low | Reliability | No automated retry for FAILED outbound WhatsApp; attempt-first SENT can die post-accept. Recovery is flow-specific nudge crons or manual. (See AD-01 for the broken admin retry.) | `lib/message-events.ts` | FAILED-message sweep cron + working resend action | messaging | P1 | No |
| OBS-11 | Low | Obs | Daily funnel reports are manual CLI only (cron wiring deferred); LEAD WorkflowEvents keep jobRequestId in unindexed JSON. | `scripts/daily-customer-funnel-report.ts:5-7` | Wire to cron + ADMIN_WHATSAPP digest | platform | P2 | No |
| OBS-12 | Low | Obs | Two admin audit writes bypass crudAction (direct adminAuditEvent.create, no paired AuditLog). | `app/(admin)/admin/applications/page.tsx:854,908` | Migrate to crudAction | admin | P2 | No |

## Architecture / Engineering (ARC)

| ID | Sev | Type | Finding | Evidence | Recommended fix | Owner | Pri | Blocks |
|---|---|---|---|---|---|---|---|---|
| ARC-01 | High | Architecture | No central state machine for Lead (16 states, 13 writer files) and JobRequest (9 states, ~20 writer files); several blind status writes in concurrent matching paths. Race-window illegal transitions, silent funnel corruption. | `lib/matching/service.ts:2707,2735,2770,3353`, `lib/matching-engine.ts:256`, `lib/lead-unlocks.ts:337,541` | Extend `lib/jobs.ts` pattern: transition maps + CAS transition functions | matching | P1 | Yes |
| ARC-02 | High | Architecture | House error envelope adopted by ~10–18 of 110 routes; 426 raw NextResponse.json calls; three envelope shapes coexist. | `lib/api-response.ts:1-10`, `lib/api-auth.ts:22-36` | Finish migration; lint rule banning raw error bodies in app/api | platform | P2 | No |
| ARC-03 | High | Architecture | Client error boundaries console.error only despite valid Sentry configs; 8 captureException calls app-wide. (Compounds OBS-01.) | `app/error.tsx:20` | captureException in every error.tsx; wire apiError 5xx to Sentry | platform | P0 | Yes |
| ARC-04 | High | Architecture | Support `reference_id` random, never correlated to `lib/correlation.ts`; `application-error-service.ts` (traceId, publicRef, persistence) used in 2 files. Support IDs untraceable. | `lib/api-response.ts:31-35` | Thread correlationId into the envelope; single capture path | platform | P1 | No |
| ARC-05 | Medium | Data | `lib/payments.ts:492` transitions Booking + writes BookingStatusEvent manually, bypassing the transition map. | `lib/payments.ts:492` | Call transitionBooking from the payment flow | payments | P1 | No |
| ARC-06 | Medium | Data | `Dispute.jobId` bare string — no FK/relation to Job; `raisedByRole` string not enum. Orphan disputes possible on a trust-critical table. | `prisma/schema.prisma:1867-1883` | Additive migration: FK + enum | platform | P2 | No |
| ARC-07 | Medium | Data | Migration hygiene scars: ~7 repair/force_reapply migrations post-restore; raw-SQL-only indexes absent from schema.prisma. `migrate diff` permanently reports drift; schema.prisma not the full truth. | `prisma/migrations/20260606115900_repair_production_schema_drift`, `20260421040000:21` | Represent FK indexes in schema; drift-allowlist doc; keep db:probe-migrations in CI | platform | P2 | No |
| ARC-08 | Medium | Performance | N+1 in matching hot paths (per-item awaited DB calls in loops). Matching/recontact latency linear with volume; cron overruns at ~10×. | `lib/matching/customer-recontact.ts:621-627`, `lib/matching/orchestrator.ts:316-330`, `service.ts:1871-1903` | Batch createMany/updateMany + preloaded maps | matching | P1 | Yes (10×) |
| ARC-09 | Medium | Architecture | `lib/whatsapp-bot.ts` 4,909-line god-module with circular-dep workaround; lib/ is a 218-entry flat namespace. | `lib/whatsapp-flows/job-request.ts:77` | Split bot into router + flow modules; domain folders in lib/ | messaging | P2 | No |
| ARC-10 | Medium | Architecture | No env validation layer: 193 raw process.env reads in lib/; misconfig found at call time; inconsistent fail-open/closed. | grep count | Zod-validated `lib/env.ts` at instrumentation time | platform | P1 | No |
| ARC-11 | Medium | Architecture | API input validation mostly manual (~8/110 routes with zod). (= SRE-09) | route sample | `parseBody(schema)` helper + house envelope | platform | P2 | No |
| ARC-12 | Low | Data | Provider status admin action accepts any target enum, no from-state validation; kycStatus has a direct admin override outside the verification machine. | `app/(admin)/admin/providers/actions.ts:335,486` | VALID_PROVIDER_STATUS_TRANSITIONS with documented admin-override edges | admin | P2 | No |
| ARC-13 | Low | Architecture | Server-action convention split: 35 actions.ts vs 61 inline 'use server' files; crudAction in 31 files — house rule not mechanically enforced. | repo grep | Codemod + eslint restriction | platform | P3 | No |
| ARC-14 | Low | Architecture | Dependency issues: duplicate Radix paths, legacy smile-identity-core alongside Didit, heavy static deps (mermaid, @react-pdf/renderer, motion), exceljs in devDeps (verify no runtime import). | `field-service/package.json` | Prune + dynamic-import heavy deps | platform | P3 | No |
| ARC-15 | Low | Performance | Admin bookings list: take 200 with 4-level nested include. | `app/(admin)/admin/bookings/page.tsx:46,64` | Select projection + cursor pagination | admin | P3 | No |
| ARC-16 | Low | Architecture | Public-surface hardening: `auth/phone-exists` enumeration surface (note: SEC verified it's neutered — always 404); `customer/notify-interest` unvalidated public POST; payment-intent status route relies on in-action ownership. | route files | Rate-limit + route-level auth | security | P2 | No |
| ARC-17 | Info | Testing | Test depth uneven: wallet/ledger/credit strong, voucher (6) and invoice (2) thin; Playwright smoke doesn't run on PRs. | `__tests__` inventory | Invoice tests; smoke on PRs vs preview URL | platform | P2 | No |

## Security / Data Protection (SEC)

| ID | Sev | Type | Finding | Evidence | Recommended fix | Owner | Pri | Blocks |
|---|---|---|---|---|---|---|---|---|
| SEC-01 | High | Security | `ProviderApplication.idNumber` stored in plaintext — schema carries its own POPIA §26 "encrypt before GA" TODO, and GA shipped 2026-07-04. Regulatory exposure on government identifiers. | `prisma/schema.prisma:391-392` | Encrypt at rest (AES-GCM helper exists in `lib/identity-verification/crypto.ts`), backfill, purge where Didit holds identity | security | P0 | Yes |
| SEC-02 | Medium | Security | Peach webhook fails open when secret unset (HMAC with `''`). Forged payment.success possible on misconfig; mitigated by bookingId+amount+idempotency, but a customer knows their own bookingId/amount. (= SRE-07) | `lib/payments.ts:111,162` | Fail closed on empty secret; verify prod env today | security | P0 | Config-dep |
| SEC-03 | Medium | Data | Customer/job media on PUBLIC Vercel Blob (job photos, evidence, quote attachments, profile + registration evidence); raw blob URLs persisted and circulated. Leaked URL = fetchable forever, no revocation. KYC docs correctly private (Supabase bucket). | `lib/storage.ts:51,86,122,166,181`, `lib/job-request-access.ts:158-161` | Private Blob + read-through `/api/attachments/[id]` proxy (already does ownership scoping); stop returning raw URLs | security | P1 | No |
| SEC-04 | Low | Security | Proxy matcher skips any path ending in an image extension (`/admin/x.png` bypasses middleware). Currently mitigated by layout-level requireAdmin/requireProvider + self-authenticating APIs; risk is future routes relying on proxy alone. | `proxy.ts:422` | Scope extension exclusion to `_next/` + asset prefixes | security | P2 | No |
| SEC-05 | Low | Security | Quote approvalToken never expires/revokes; GET serves quote details + suburb indefinitely. (= CJ-23) | `app/api/quotes/[token]/route.ts:22-84` | 404 after decision + N days | security | P2 | No |
| SEC-06 | Low | Security | Bearer tokens stored plaintext in DB (customerAccessToken, approvalToken); registration resume tokens correctly SHA-256 hashed. Read-only DB leak yields live customer links. | `lib/job-request-access.ts:19,70` vs `lib/provider-registration/tokens.ts` | Store SHA-256, look up by hash | security | P3 | No |
| SEC-07 | Low | Security | CRON_SECRET compared with `!==` (non-constant-time) across /api/cron/* and /api/internal/*. Impractical over network; inconsistent with codebase's own timingSafeEqual standard. | `app/api/cron/match-leads/route.ts:38-39` | Shared `verifyCronSecret()` with timingSafeEqual | security | P3 | No |
| SEC-08 | Low | Data | Three sites log raw phone numbers (rest of the codebase uses hashes/masks; no idNumber logging found). | `lib/whatsapp-flows/job-request.ts:1980`, `lib/worker-provider-auth.ts:135`, `lib/job-requests/create-job-request.ts:387` | Mask to last-4/hash at these three sites | security | P2 | No |
| SEC-09 | Low | Abuse/Avail | Rate-limit posture depends on Upstash env at runtime: OTP fails CLOSED when Redis unavailable (legit sign-in bricked); memory fallback dev-only. Security-sweep memory notes 3 Vercel env vars still open. | `lib/rate-limit.ts` | Confirm Upstash vars in prod; alert on rate_limit.degraded | security | P0 | If env missing |

**Verified-OK security posture (for the record):** all webhooks except the Peach empty-secret edge are HMAC-verified with timingSafeEqual; RLS enabled across effectively all tables (client anon key deny-by-default; all data access server-side Prisma); sb-access-token HttpOnly/SameSite=Lax/Secure/24h cap; admin RBAC has DB-backed proxy checks, last-OWNER + self-guards, OWNER-gated hard deletes; IDOR spot-checks passed on 8 surfaces; uploads have MIME allowlist + magic-byte sniffing + 10MB cap; KYC docs in private bucket with a "must be private" guard; no dangerous HTML/query injection found; no hardcoded secrets; `/api/debug/*` hard-403 in prod.

## Testing (TST) — from checks run during this audit

| ID | Sev | Type | Finding | Evidence | Recommended fix | Owner | Pri | Blocks |
|---|---|---|---|---|---|---|---|---|
| TST-01 | — | Verified | Lint, `prisma validate`, `tsc --noEmit`, 5,164 unit tests (511 files), and production build all pass on audit day. | `/tmp/pap-audit-checks.log`, build log | — | — | — | No |
| TST-02 | Low | Testing | `tsc` showed 24 errors from a stale generated Prisma client until `prisma generate` was run — nothing in CI or postinstall guarantees client freshness for local typecheck. | tsc run 2026-07-06 | `prisma generate` in postinstall or pretest | platform | P3 | No |
| TST-03 | Medium | Testing | Playwright smoke (18 tests) exists but runs only on push with env-gated secrets — not on PRs; e2e needs a deployed URL + admin creds not available locally. Live prod spot-check (8 public routes, read-only GETs) returned 200 on audit day; admin domain correctly 307s. | `.github/workflows/field-service-ci.yml`, curl results | Run smoke vs preview deployments on PRs | platform | P1 | No |
| TST-04 | Medium | Testing | Zero smoke coverage of customer token surfaces (= CJ-25); invoice/voucher unit depth thin (= ARC-17). | see CJ-25, ARC-17 | Add invalid-token smokes + invoice tests | platform | P1 | No |

## Provider / Technician Journey (PJ)

| ID | Sev | Type | Finding | Evidence | Recommended fix | Owner | Pri | Blocks |
|---|---|---|---|---|---|---|---|---|
| PJ-01 | Critical | Defect | **Approved ≠ matchable, silently:** approval flips verified/active/ACTIVE but never creates `TechnicianServiceArea` rows (both approve paths call `syncProviderRecord` without `locationNodeIds`); non-matching-region rows created inactive; structured addresses hard-NO_MATCH before legacy string fallback. Providers pass onboarding and silently never receive leads. | `app/(admin)/admin/applications/page.tsx:355-366`, `lib/provider-auto-approve.ts:857-868`, `lib/provider-record.ts:131-160`, `lib/matching/filter.ts:250` | Approval-time TSA sync + matchability-readiness check that runs filter reason codes and notifies | matching | P0 | Yes |
| PJ-02 | High | Defect | KYC gate drops any post-2026-06-11 provider with kycStatus≠VERIFIED (grace admits legacy cohort only); the KYC nudge cron targets **only** the legacy cohort — post-cutoff unmatchables get nothing. No notification exists for verified=false, missing TSA, or CATEGORY_NOT_APPROVED; reason codes are admin-only. | `lib/matching/filter.ts:349-360`, `lib/matching/kyc-grace.ts:20,61-76`, `lib/kyc-drive/nudge.ts:3-6,94-96` | Extend KYC nudge to post-cutoff cohort; provider-facing readiness status in PWA | matching | P1 | Yes |
| PJ-03 | High | Defect | Cert-gated matching can never pass organically: filter queries `TechnicianCertification` (zero writers repo-wide) and verified `ProviderCertification` (only writer = admin manual entry). Signup `certificationRef` never promoted to rows. Electrical jobs match no one without hand-entry. | `lib/matching/filter.ts:178-197,382-421`, `app/(admin)/admin/providers/actions.ts:685` | Promote application cert captures to ProviderCertification on approval (with verification step) | matching | P1 | Yes (regulated trades) |
| PJ-04 | High | Security/Safety | High-risk cert enforcement asymmetry: evidence enforced only at signup when qgv2 ON (default OFF); admin manual approve has no cert check; auto-approve merely skips high-risk; only electrical of 9 high-risk categories carries requiredCertificationCodes — plumbing/gas/geyser have empty arrays; certificationRef accepts any text. | `lib/service-category-policy.ts:60-263`, `lib/provider-applications-submit.ts:145-159`, `lib/provider-auto-approve.ts:787-791` | Define cert codes for all high-risk categories once PJ-03 fixed; validate cert refs at review | security | P1 | Yes (liability) |
| PJ-05 | High | UX/Funnel | PWA drafts have zero re-engagement: recovery cron builds its queue solely from WhatsApp Conversation data; `ProviderApplicationDraft` never read. Abandoned OTP-verified web applicants are dead ends. | `lib/provider-onboarding-recovery.ts:641-693` | Draft-based recovery lane (WhatsApp template to draft phone) | matching | P1 | Yes |
| PJ-06 | High | UX/Funnel | WhatsApp recovery classifier stale: ~11 deep-funnel steps (rates→references→certification→awaiting_kyc) fall through to `welcome_idle` and get the cold "reply REGISTER" intro; Didit link issued-but-never-clicked falls between the renudge crons; recovery flags default report-only. | `lib/provider-onboarding-recovery.ts:274-306,413-422`, `app/api/cron/identity-verification-in-flight-renudge/route.ts:80-88` | Extend classifier to all reg_* steps; add link-never-clicked cohort | matching | P1 | Yes |
| PJ-07 | High | Reliability | Customer sign-off dead-end (provider seat of CJ-11): one 48h completion link sent once; no re-nudge, no auto-complete, no admin force-complete; PENDING_COMPLETION_CONFIRMATION persists indefinitely (only an ops SLA alert notices). | `lib/jobs.ts:26,207-230`, `lib/job-completion-access.ts:12`, `lib/ops-dashboard/alerts.ts:150-156` | 24h re-nudge + auto-complete after N days + admin force-complete action | matching | P1 | Yes |
| PJ-08 | High | UX | Post-top-up resume gap: insufficient-credits parks the lead in CREDIT_REQUIRED (resumable), but the payment-received message contains no link back to re-accept; the 24h window keeps ticking. Providers pay for credits then lose the lead that prompted the purchase. | `lib/provider-credit-check.ts:320-420`, `lib/provider-wallet-notifications.ts:164` | Append pending-lead CTA to the credit-confirmation template | matching | P1 | Yes |
| PJ-09 | High | Data | Earnings/payout is a read-only facade: `ProviderPayout` has zero writers; `payoutVerifiedAt` never written yet read by tier gating (gates can never pass); earnings dashboard permanently empty. | `prisma/schema.prisma:1344-1362`, `app/(provider)/provider/earnings/page.tsx`, `lib/provider-tier.ts:77-78,127` | Hide the surface or compute from completed-job quotes; decide payout story before scale | payments | P1 | Yes (trust) |
| PJ-10 | Medium | UX | Suspension/strike/ban with zero outbound notification; provider discovers on next inbound ("profile is inactive"). No appeal flow; `strikes` drives nothing automatic. | `app/(admin)/admin/providers/actions.ts:295-631`, `lib/whatsapp-identity.ts:333-336` | Notify with reason + reinstatement path; structured appeal | admin | P2 | No |
| PJ-11 | Medium | Architecture | Gate-ON submit logic triplicated across WhatsApp, PWA, web-resume — payloads already drift (PWA hardcodes hourlyRate:null). Guard drift before qgv2 flips ON. | `lib/whatsapp-flows/registration.ts:2694-2826`, `lib/provider-registration/pwa-flow.ts:524-718`, `app/provider/signup/actions.ts:38-196` | Extract shared submission service | platform | P1 | No |
| PJ-12 | Medium | Data | Channel-divergent capture: PWA never captures ID number; web-resume ID lacks Luhn; gate-OFF PWA submit drops certificationRef/references/bio/hourlyRate; `evidenceFileUrls` mixes attachment IDs and blob URLs in one column. | `lib/web-signup-sections.ts:19-20`, `pwa-flow.ts:648-677,827-848` | Normalise payload schema + evidence storage | platform | P2 | No |
| PJ-13 | Medium | Defect | Completion evidence enforced only on WhatsApp: PWA "Mark complete" requires no note/photo; lead-page markJobComplete jumps straight to COMPLETED, bypassing customer sign-off and firing the review request. | `components/provider/StatusControls.tsx:48-52`, `lib/accepted-job-actions.ts:560-624` | Enforce note in transitionJob; route lead-page complete through PENDING confirmation | matching | P2 | No |
| PJ-14 | Medium | UX | No lead history: PWA leads list shows SENT/VIEWED only; accepted/expired/declined vanish. Shortlist leads dying at the 24h window get no expiry push (dispatch-path expiry IS notified). Providers can't reconcile credits vs outcomes. | `lib/provider-lead-list.ts:29-34`, `lib/provider-accepted-lock.ts:511-518` | Lead-history page + shortlist expiry notice | matching | P2 | No |
| PJ-15 | Medium | UX | Top-up is Pay@ retail-till only (physical store, R7 fee); KYC-verified required before purchase; late-accept grace applies to dispatch path only (shortlist has hard 24h cutoff). | `lib/provider-journey.ts:1832-1917`, `lib/identity-verification/credit-gate.ts:80,175` | Add an instant payment method | payments | P2 | No |
| PJ-16 | Low | UX | Job commands taught only reactively; stale `pj_upd_` buttons call transitionJob uncaught (raw errors); PWA verify-step copy stale under gate ON; summary edit menu covers 6 of ~15 fields. | `lib/whatsapp-bot.ts:2469-2480`, `lib/provider-journey.ts:1467-1473` | Command cheat-sheet at assignment; try/catch button path | messaging | P3 | No |

**Bright spots (verified):** insufficient-credits handling is honest and safe (no deduction, contact stays locked, retry button); dispatch-path expiry + lost-to-another-provider notices exist; wrong-state WhatsApp command errors are friendly and idempotent; WhatsApp completion capture (note+photo) is solid; qgv2 create-on-PASS design has defence-in-depth re-checks.
