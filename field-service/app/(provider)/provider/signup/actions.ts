'use server'

// Stub — Task 12 will replace this with the real implementation.
export async function submitProviderApplicationFromWebAction(_input: {
  rawToken: string
  payload: unknown
}): Promise<{ ok: true; applicationId: string }> {
  throw new Error('not implemented — see Task 12')
}
