# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Per-app ignores are in each app's own .gitignore

# MCP tooling
.playwright-mcp/

# Local package manager caches
.pnpm-store/

# Local test artifacts
field-service/test-results/

# Voucher export files contain raw single-use codes — never commit, anywhere in the tree.
# The per-app .gitignore in field-service/ also covers this; this is belt-and-braces.
vouchers*.csv
vouchers*.xlsx
**/vouchers*.csv
**/vouchers*.xlsx

# Local cleanup archive for untracked/generated artifacts that should not be
# committed without an explicit product/docs review.
.local-artifacts/

# Git worktrees
.worktrees/

# Media source files (raw assets — processed outputs go into app public/ dirs)
media assets/
.vercel
